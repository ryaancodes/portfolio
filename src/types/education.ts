export interface EducationItem {
  id: string;
  period: string;
  title: string;
  institution: string;
  detail: string;
}

export interface AchievementItem {
  id: string;
  value: string;
  label: string;
}
